import { compile } from 'https://cdn.jsdelivr.net/gh/spideythedev/simple.js@main/packages/compiler/src/index.js';

const scripts = document.querySelectorAll('script[type="text/simplejs"]');

for (const script of scripts) {
  const js = compile(script.textContent);
  new Function(js)();
}